apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sdf-home
spec:
  accessModes:
  - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: sdf-home
status:
  accessModes:
  - ReadOnlyMany
  capacity:
    storage: 1Gi

